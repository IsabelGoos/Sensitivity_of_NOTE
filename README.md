"filename" -> ROOT output by EarthProbe
Possible "channel"s:
h_*_**
with * = tracks
         showers
         int
and ** = all (not for * = int)
         (a)nue_cc
         (a)numu_cc
         (a)nutau_cc
         (a)nu_nc
         muatm
or h_osc_*_**
with * = (a)nue
         (a)numu
and ** = (a)nue
         (a)numu
         (a)nutau 
g_sampling_points
